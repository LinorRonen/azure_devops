trigger:
- main  

pool:
  name: Default  

steps:
- task: UseDotNet@2
  displayName: 'Install .NET SDK'
  inputs:
    packageType: 'sdk'
    version: '6.x'
    installationPath: $(Agent.ToolsDirectory)/dotnet

- script: |
    dotnet build ./MyHelloWorld/MyHelloWorld.csproj --configuration Release
  displayName: 'Build the Project'

- script: dotnet test
  displayName: 'Run Tests'

- script: |
    dotnet pack ./MyHelloWorld/MyHelloWorld.csproj --configuration Release --output $(Build.ArtifactStagingDirectory)
  displayName: 'Package the Project'

- task: PublishBuildArtifacts@1
  displayName: 'Publish Artifact to Azure DevOps'
  inputs:
    pathToPublish: $(Build.ArtifactStagingDirectory)
    artifactName: 'drop'
